import telebot
API_TOKEN = '8889728091:AAHbLgJQFGvrmTIrW7k2Ro1aTB3kjcYHc_A'

bot = telebot.TeleBot(API_TOKEN)

# /start buyrug'i kelganda bot beradigan javob
@bot.message_handler(commands=['start'])
def send_welcome(message):
    welcome_text = (
        "👋 Assalomu alaykum! Dachabuloq botiga xush kelibsiz!\n\n"
        "Sizga qanday yordam bera olaman? Tezkunda yangi funksiyalar qo'shiladi."
    )
    bot.reply_to(message, welcome_text)

# Foydalanuvchi har qanday matn yozganda qaytarib javob berish (Exo funksiyasi)
@bot.message_handler(func=lambda message: True)
def echo_all(message):
    bot.reply_to(message, f"Siz yozdingiz: {message.text}")

# Botni uzluksiz ishlatib turish buyrug'i
if __name__ == '__main__':
    print("Bot ishga tushdi...")
    bot.infinity_polling()
